---
phase: 03-body-handling-public-api-surface
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tsconfig.test.json
  - package.json
autonomous: true
requirements: [API-03]
tags: [tsconfig, supertest, devdeps, typecheck, infrastructure]

must_haves:
  truths:
    - "`npx tsc --noEmit -p tsconfig.test.json` exits 0 and DOES include `src/**/*.test.ts` in its compilation graph (so future `// @ts-expect-error` directives in src/types.test.ts will actually be checked)."
    - "`supertest` and `@types/supertest` are installed under `devDependencies` and resolvable from `node_modules`."
    - "`npm run typecheck` is a defined script and runs `tsc --noEmit -p tsconfig.test.json`."
    - "`npm test` continues to exit 0 (Phase 2 baseline preserved — 21 tests across 4 files)."
    - "Per D-21: supertest install in this plan is the foundational scope for Phase 3 integration-shape tests; Phase 6 owns the cross-cutting Supertest coverage gate."
  artifacts:
    - path: "tsconfig.test.json"
      provides: "TypeScript test config that extends tsconfig.json and removes the **/*.test.ts exclude (D-20 + RESEARCH.md Pitfall 1)"
      contains: '"extends": "./tsconfig.json"'
    - path: "package.json"
      provides: "typecheck script + supertest devDependencies"
      contains: '"typecheck"'
  key_links:
    - from: "tsconfig.test.json"
      to: "tsconfig.json"
      via: "extends field"
      pattern: '"extends":\\s*"\\./tsconfig\\.json"'
    - from: "package.json scripts.typecheck"
      to: "tsconfig.test.json"
      via: "tsc --noEmit -p flag"
      pattern: 'tsc --noEmit -p tsconfig\\.test\\.json'
---

<objective>
Wave 0 test infrastructure: ship the `tsconfig.test.json` and supertest devDependencies that every subsequent Phase 3 plan depends on. Without this, the compile-time discriminated-union test (`src/types.test.ts`, Plan 05) silently skips, breaking the SC3 guarantee, and integration-shape tests (Plans 03, 04, 06) cannot import `supertest`.

Purpose: Close the two infrastructure gaps documented in `03-VALIDATION.md` Wave 0 Requirements and `03-RESEARCH.md` Pitfall 1.
Output: `tsconfig.test.json` (new), `package.json` (modified — adds `typecheck` script + 2 devDependencies).
</objective>

<execution_context>
@/home/taylor_david_ray/src/github.com/cornjacket/ai-sandbox/gsd-walkthru/.claude/get-shit-done/workflows/execute-plan.md
@/home/taylor_david_ray/src/github.com/cornjacket/ai-sandbox/gsd-walkthru/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-body-handling-public-api-surface/03-CONTEXT.md
@.planning/phases/03-body-handling-public-api-surface/03-RESEARCH.md
@.planning/phases/03-body-handling-public-api-surface/03-PATTERNS.md
@.planning/phases/03-body-handling-public-api-surface/03-VALIDATION.md
@tsconfig.json
@package.json

<interfaces>
<!-- Existing tsconfig.json (REFERENCE — DO NOT modify): -->
```jsonc
// tsconfig.json — current state (Phase 1 baseline)
{
  "compilerOptions": {
    "target": "ES2020",
    "module": "ESNext",
    "lib": ["ES2020"],
    "types": ["node"],
    "moduleResolution": "bundler",
    "strict": true,
    "esModuleInterop": true,
    "declaration": true,
    "declarationMap": true,
    "sourceMap": true,
    "skipLibCheck": true,
    "ignoreDeprecations": "6.0",
    "outDir": "dist"
  },
  "include": ["src"],
  "exclude": ["node_modules", "dist", "**/*.test.ts"]
}
```

<!-- Existing package.json scripts block (REFERENCE — extend, do not rewrite): -->
```jsonc
"scripts": {
  "build": "tsup",
  "test": "vitest run",
  "lint": "biome check .",
  "format": "biome format --write ."
}
```

<!-- Existing devDependencies block (REFERENCE — extend, do not rewrite): -->
```jsonc
"devDependencies": {
  "@biomejs/biome": "^2.4.14",
  "@types/express": "^4.17.21",
  "@types/node": "^20.0.0",
  "tsup": "^8.5.1",
  "typescript": "^6.0.3",
  "vitest": "^4.1.5"
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tsconfig.test.json and add typecheck script</name>
  <files>tsconfig.test.json, package.json</files>
  <read_first>
    - tsconfig.json (current Phase 1 baseline — confirm `**/*.test.ts` is in exclude)
    - package.json (current scripts block + devDependencies — extend in place; preserve formatting)
    - .planning/phases/03-body-handling-public-api-surface/03-RESEARCH.md (Pitfall 1, lines ~739-773 — explains why this config is required)
    - .planning/phases/03-body-handling-public-api-surface/03-VALIDATION.md (Wave 0 Requirements bullet — typecheck script command)
  </read_first>
  <action>
    Create the new file `tsconfig.test.json` at the repo root with EXACTLY this content (matches RESEARCH.md Pitfall 1 verbatim — tsconfig is JSON-with-comments, jsonc):

    ```jsonc
    {
      "extends": "./tsconfig.json",
      "include": ["src"],
      "exclude": ["node_modules", "dist"]
    }
    ```

    Why: the main `tsconfig.json` excludes `**/*.test.ts`, so `tsc --noEmit -p tsconfig.json` silently skips test files — which means the `// @ts-expect-error` directives Plan 05 will write into `src/types.test.ts` are not actually checked. By extending the base config and replacing the `exclude` array (no `**/*.test.ts` entry), `tsconfig.test.json` includes the test files. (D-20 / RESEARCH.md Pitfall 1.)

    Then modify `package.json`:
    1. Add a new entry to the `scripts` block, alphabetically between `"format"` and `"lint"` (Biome's organizeImports convention does NOT apply to package.json, but alphabetizing keeps diffs minimal):
       ```jsonc
       "typecheck": "tsc --noEmit -p tsconfig.test.json"
       ```
       Final scripts block (exact target state):
       ```jsonc
       "scripts": {
         "build": "tsup",
         "format": "biome format --write .",
         "lint": "biome check .",
         "test": "vitest run",
         "typecheck": "tsc --noEmit -p tsconfig.test.json"
       }
       ```
       (If existing keys were not alphabetized, leave them in their current positions and just append `"typecheck"` after the existing keys — do not reorder unrelated keys.)

    Do NOT touch any other field in package.json (devDependencies handled in Task 2).
    Do NOT create `src/express.d.ts` — RESEARCH.md Pattern 2 explicitly chooses module-file augmentation in `src/types.ts` (Plan 05), not a pure ambient `.d.ts`.
  </action>
  <verify>
    <automated>test -f tsconfig.test.json &amp;&amp; node -e "const c=require('./tsconfig.test.json'); if(c.extends!=='./tsconfig.json') process.exit(1); if(JSON.stringify(c.exclude)!==JSON.stringify(['node_modules','dist'])) process.exit(2); if(JSON.stringify(c.include)!==JSON.stringify(['src'])) process.exit(3);" &amp;&amp; node -e "const p=require('./package.json'); if(p.scripts.typecheck!=='tsc --noEmit -p tsconfig.test.json') process.exit(4);"</automated>
  </verify>
  <acceptance_criteria>
    - File `tsconfig.test.json` exists at repo root.
    - `tsconfig.test.json` parses as valid JSON (jsonc with no comments is valid JSON).
    - `tsconfig.test.json` `extends` field equals `"./tsconfig.json"`.
    - `tsconfig.test.json` `exclude` array equals exactly `["node_modules", "dist"]` (NO `**/*.test.ts` entry).
    - `tsconfig.test.json` `include` array equals exactly `["src"]`.
    - `package.json` `scripts.typecheck` equals exactly `"tsc --noEmit -p tsconfig.test.json"`.
    - `node -e "console.log(require('./package.json').scripts.typecheck)"` outputs `tsc --noEmit -p tsconfig.test.json`.
    - Existing scripts (`build`, `test`, `lint`, `format`) preserved unchanged.
    - Existing tsconfig.json unchanged: `grep -c '\*\*/\*\.test\.ts' tsconfig.json` returns 1 (still excluded from prod build).
    - `npx tsc --noEmit -p tsconfig.test.json` exits 0 (the existing source compiles cleanly under the test config; Phase 2 baseline test files like `src/errors.test.ts` are now in the graph and must still compile).
  </acceptance_criteria>
  <done>
    `tsconfig.test.json` exists with the four-line jsonc body specified, `package.json` has the `typecheck` script entry, all four pre-existing scripts are preserved, `tsconfig.json` is untouched, and `npx tsc --noEmit -p tsconfig.test.json` exits 0 against the existing Phase 2 codebase.
  </done>
</task>

<task type="auto">
  <name>Task 2: Install supertest and @types/supertest as devDependencies</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - package.json (current devDependencies — confirm supertest and @types/supertest are NOT present)
    - .planning/phases/03-body-handling-public-api-surface/03-RESEARCH.md (Standard Stack §"New Dev Dependencies Phase 3 Should Add", lines ~177-195 — version-pin convention is `^X.Y.Z`)
    - .planning/research/STACK.md (Phase 1 STACK already lists supertest as a planned devDep)
  </read_first>
  <action>
    Install the two new devDependencies via npm so `package-lock.json` is updated atomically with `package.json`:

    ```bash
    npm install --save-dev supertest @types/supertest
    ```

    npm will pick the latest stable for each. This is intentional — RESEARCH.md says "run `npm view supertest version &amp;&amp; npm view @types/supertest version` per RESEARCH.md before pinning" but pinning to `^X.Y.Z` (caret-major) is the existing repo convention (see `^4.17.21`, `^2.4.14`, etc.). After install, verify the resulting entries in `package.json#devDependencies` use the caret form `^X.Y.Z`. If npm wrote a non-caret form (rare with default config — can happen with `npm config get save-prefix`), edit the two entries to caret form by hand, then run `npm install` again to refresh the lockfile.

    Final devDependencies block (alphabetized by key, like the existing block):

    ```jsonc
    "devDependencies": {
      "@biomejs/biome": "^2.4.14",
      "@types/express": "^4.17.21",
      "@types/node": "^20.0.0",
      "@types/supertest": "^X.Y.Z",
      "supertest": "^X.Y.Z",
      "tsup": "^8.5.1",
      "typescript": "^6.0.3",
      "vitest": "^4.1.5"
    }
    ```

    where `X.Y.Z` are the latest stable versions npm resolved.

    Do NOT add `supertest` to `dependencies` (it is dev-only — production consumers don't need it).
    Do NOT add `body-parser` or `raw-body` — `raw-body` is already transitively present via `express` (RESEARCH.md Standard Stack confirms `[VERIFIED: node_modules/raw-body/package.json]`); re-declaring it would risk version drift (PITFALLS #17).
    Do NOT touch `peerDependencies` — Express stays at `4.x || 5.x` exactly as Phase 1 D-13 locked it.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); if(!p.devDependencies.supertest) process.exit(1); if(!p.devDependencies['@types/supertest']) process.exit(2); if(p.dependencies &amp;&amp; p.dependencies.supertest) process.exit(3); if(p.peerDependencies.express!=='4.x || 5.x') process.exit(4);" &amp;&amp; test -d node_modules/supertest &amp;&amp; test -d node_modules/@types/supertest &amp;&amp; npm test</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` `devDependencies.supertest` is set to a string starting with `^` (caret-major version range).
    - `package.json` `devDependencies["@types/supertest"]` is set to a string starting with `^`.
    - `package.json` does NOT have `dependencies.supertest` (must be dev-only).
    - `package.json` `peerDependencies.express` equals exactly `"4.x || 5.x"` (Phase 1 D-13 unchanged).
    - `node_modules/supertest/package.json` exists and is readable.
    - `node_modules/@types/supertest/package.json` exists and is readable.
    - `package-lock.json` was updated by the npm install (file mtime is newer than before the task ran).
    - `npm test` exits 0 (Phase 2 baseline preserved; supertest install must not break anything).
    - `npx tsc --noEmit -p tsconfig.test.json` exits 0 (the new types resolve and compile in the test graph).
    - No new top-level `dependencies` were introduced (`grep -c '"dependencies"' package.json` returns 0 unless one was already there).
  </acceptance_criteria>
  <done>
    `npm install --save-dev supertest @types/supertest` has run, both packages appear in `package.json#devDependencies` with caret-major ranges, both are present under `node_modules/`, `package-lock.json` reflects the install, and the full Phase 2 test suite (`npm test`) and the new typecheck command (`npm run typecheck`) both exit 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Plan-time (build infra) | The npm registry → developer workstation → CI matrix. Affects only the build/test toolchain, no runtime code. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-INFRA-01 | Tampering | `tsconfig.test.json` exclude config | mitigate | Acceptance criteria pin `exclude` to exactly `["node_modules", "dist"]` and `include` to exactly `["src"]` — preventing accidental re-introduction of `**/*.test.ts` exclusion which would silently skip Plan 05's `@ts-expect-error` compile-time discriminated-union test. |
| T-3-INFRA-02 | Tampering | `package.json` peerDependencies / dependencies | mitigate | Acceptance criteria assert `dependencies.supertest` is absent (dev-only) and `peerDependencies.express` is unchanged; `npm install --save-dev` cannot promote a package to `dependencies` accidentally. |
| T-3-05 | Information Disclosure | Library observability surface | accept | Plan 01 modifies build infra only; no `src/` code changes — no logging surface is introduced. Threat is fully addressed by Plans 03/04/06's no-`console.*` assertions on actual code paths. |
</threat_model>

<verification>
Run after both tasks:
- `npx tsc --noEmit -p tsconfig.test.json` — exits 0
- `npm test` — exits 0
- `npm run typecheck` — exits 0 (new script wired up)
- `npm run lint` — exits 0 (Biome must not flag the modified package.json or the new tsconfig.test.json; both are JSON, not TS)
</verification>

<success_criteria>
- `tsconfig.test.json` exists at repo root, extends `./tsconfig.json`, and does NOT exclude `**/*.test.ts`.
- `package.json#scripts.typecheck` runs `tsc --noEmit -p tsconfig.test.json`.
- `supertest` + `@types/supertest` installed under `devDependencies` with caret-major version ranges.
- All four existing npm scripts (`build`, `test`, `lint`, `format`) preserved verbatim.
- `npm test`, `npm run typecheck`, `npm run lint` all exit 0 after the plan completes.
</success_criteria>

<output>
After completion, create `.planning/phases/03-body-handling-public-api-surface/03-01-SUMMARY.md` documenting:
- Final versions of supertest and @types/supertest pinned in package.json
- Confirmation that `tsconfig.test.json` includes `src/**/*.test.ts` in its compilation graph (run `npx tsc --noEmit -p tsconfig.test.json --listFiles 2>&1 | grep -c '\.test\.ts$'` and report the count — should be ≥1, currently 4 from Phase 2 baseline)
- Any deviation from the planned scripts ordering
</output>
