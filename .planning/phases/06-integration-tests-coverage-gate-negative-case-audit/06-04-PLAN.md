---
phase: "06"
plan: "04"
type: execute
wave: 2
depends_on:
  - "06-02"
  - "06-03"
files_modified:
  - .planning/phases/06-integration-tests-coverage-gate-negative-case-audit/06-VERIFICATION.md
autonomous: true
requirements:
  - QUAL-04

must_haves:
  truths:
    - "Five guard mutations were applied sequentially, each failing an integration test (not just a unit test)"
    - "After each mutation, source was reverted with git checkout -- <file> before the next mutation"
    - "After all 5 mutations, git status reports a clean working tree"
    - "06-VERIFICATION.md contains a Guard Removal Demonstrations section with a 5-row evidence table"
    - "Each table row identifies the exact file:line mutation, the failing integration test(s), and confirms revert"
  artifacts:
    - path: ".planning/phases/06-integration-tests-coverage-gate-negative-case-audit/06-VERIFICATION.md"
      provides: "SC1-SC4 phase evidence plus Guard Removal Demonstrations table"
      contains: "Guard Removal Demonstrations"
  key_links:
    - from: "06-VERIFICATION.md Guard Removal Demonstrations"
      to: "tests/integration/{stripe,github,shopify}.test.ts"
      via: "each mutation must fail an integration test to count as evidence"
      pattern: "integration"
---

<objective>
Perform the 5-step sequential guard mutation experiment (D-17..D-19) and produce the `06-VERIFICATION.md` file with the complete Guard Removal Demonstrations evidence table plus SC1-SC4 phase verification.

Purpose: Satisfies ROADMAP SC3 — "demonstrably fails when each guard is removed" — by verifying that each of the five critical security guards is covered by an integration test that fails when the guard is removed. The VERIFICATION.md is the deliverable; the mutation edits leave zero trace in git history.

Output: `06-VERIFICATION.md` committed to `.planning/phases/06-integration-tests-coverage-gate-negative-case-audit/`. Clean git working tree after the experiment.

Depends on: Plans 02 and 03 complete (integration tests exist, audit fixes applied, all tests passing).
</objective>

<execution_context>
@/home/taylor_david_ray/src/github.com/cornjacket/ai-sandbox/gsd-walkthru/.claude/get-shit-done/workflows/execute-plan.md
@/home/taylor_david_ray/src/github.com/cornjacket/ai-sandbox/gsd-walkthru/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/taylor_david_ray/src/github.com/cornjacket/ai-sandbox/gsd-walkthru/.planning/ROADMAP.md
@/home/taylor_david_ray/src/github.com/cornjacket/ai-sandbox/gsd-walkthru/.planning/STATE.md

<interfaces>
<!-- The five guards being mutated (exact file/line targets from D-17): -->

Mutation 1 — HMAC compare in src/providers/stripe.ts:
  Current line (approx line 109): `if (timingSafeCompare(computed, segmentBuf)) { signatureMatched = true; }`
  Mutation: change condition to `if (true) { signatureMatched = true; }` (always matches)
  Expected integration failure: Stripe tampered-body test in tests/integration/stripe.test.ts

Mutation 2 — timingSafeCompare in src/crypto/compare.ts:
  Current: the function returns the result of `crypto.timingSafeEqual` or `false`
  Mutation: replace the function body to `return true;` (always returns true)
  Expected integration failure: any wrong-secret test — Stripe/GitHub/Shopify tampered-body or wrong-secret integration tests

Mutation 3 — missing-header guard in src/providers/stripe.ts:
  Current (after Plan 03 fix): `if (header === undefined) { throw new WebhookValidationError({ reason: 'missing_header', ... }) }`
  Mutation: comment out the entire `if (header === undefined)` block
  Expected integration failure: Stripe missing-header integration test (expects 401, gets 200 or a different error)

Mutation 4 — age/tolerance check in src/providers/stripe.ts:
  Current (approx line 125): `if (age > toleranceSeconds) { throw new WebhookValidationError({ reason: 'timestamp_too_old', ... }) }`
  Mutation: comment out the entire `if (age > toleranceSeconds)` block
  Expected integration failure: Stripe replay integration test (expects 401, gets 200)

Mutation 5 — base64 decode in src/providers/shopify.ts:
  Current: `Buffer.from(header, 'base64')`
  Mutation: change to `Buffer.from(header, 'hex')`
  Expected integration failure: Shopify valid-base64-signature integration test (now wrong encoding, expects 200, gets 401) OR the hex-encoding test's expected behavior changes
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify prerequisite state and baseline test pass</name>
  <files></files>
  <read_first>
    - tests/integration/stripe.test.ts (confirm file exists and contains the replay test and missing-header test)
    - tests/integration/shopify.test.ts (confirm file exists and contains the hex-encoding test)
    - .planning/phases/06-integration-tests-coverage-gate-negative-case-audit/06-CONTEXT.md (D-17..D-19 mutation discipline)
  </read_first>
  <action>
Before beginning any mutation, verify the environment is clean and all tests pass.

Run the following sequence:

1. Confirm all three integration files exist:
   ```
   ls tests/integration/stripe.test.ts tests/integration/github.test.ts tests/integration/shopify.test.ts
   ```

2. Confirm git working tree is clean (no uncommitted changes):
   ```
   git status
   ```
   If the tree is NOT clean, do NOT proceed — surface the issue to the user.

3. Run the full test suite to establish the GREEN baseline:
   ```
   npm test
   ```
   All tests must pass. If any tests fail, do NOT proceed — the mutation experiment requires a green baseline.

4. Record the test count from the passing run (e.g., "127 tests passing across 16 files").

No file writes in this task. This is a precondition check.
  </action>
  <verify>
    <automated>npm --prefix /home/taylor_david_ray/src/github.com/cornjacket/ai-sandbox/gsd-walkthru test 2>&1 | grep -E "passed|failed"</automated>
  </verify>
  <acceptance_criteria>
    - All three integration test files exist
    - `git status` reports clean working tree (no uncommitted modifications)
    - `npm test` exits 0 with all tests passing
    - Baseline test count recorded for comparison during mutations
  </acceptance_criteria>
  <done>Green baseline confirmed; git tree clean; ready to begin mutation experiment</done>
</task>

<task type="auto">
  <name>Task 2: Execute 5 sequential guard mutations and collect evidence</name>
  <files></files>
  <read_first>
    - src/providers/stripe.ts (read before Mutations 1, 3, 4 — need exact current line content)
    - src/crypto/compare.ts (read before Mutation 2 — need exact current function body)
    - src/providers/shopify.ts (read before Mutation 5 — need exact Buffer.from line)
  </read_first>
  <action>
Execute each of the 5 mutations in order following the D-19 discipline: edit → `npm test` → capture failing test name(s) → `git checkout -- <file>` → verify clean → next mutation.

**CRITICAL D-19 RULES:**
- Do NOT commit any mutation.
- After EACH mutation, run `git checkout -- <mutated-file>` to revert.
- After the revert, run `git status` to confirm the file is clean before the next mutation.
- After ALL 5 mutations, run `git status` — MUST report clean working tree.
- If `npm test` does NOT fail during a mutation, capture that as evidence (unexpected: means an integration test is missing) and report it in the VERIFICATION.md table as "no failing test — coverage gap".

**Mutation 1: Comment out HMAC compare in stripe.ts**

Step 1a: Read `src/providers/stripe.ts`. Find the line approximately:
```typescript
if (timingSafeCompare(computed, segmentBuf)) {
  signatureMatched = true;
  // Continue loop — do NOT break; avoid timing leaks on rotation
}
```
Change the condition from `timingSafeCompare(computed, segmentBuf)` to `true`:
```typescript
if (true) {
  signatureMatched = true;
  // Continue loop — do NOT break; avoid timing leaks on rotation
}
```

Step 1b: Run `npm test`. The tampered-body integration test in `tests/integration/stripe.test.ts` MUST fail (expects 401, gets 200 since every signature now "matches").

Step 1c: Record: which test(s) failed, exact test name(s).

Step 1d: `git checkout -- src/providers/stripe.ts`

Step 1e: `git status` — confirm clean.

---

**Mutation 2: Make timingSafeCompare always return true**

Step 2a: Read `src/crypto/compare.ts`. Find the function body. Replace the return statement(s) so the function always returns `true`. For example, if the function currently contains `return crypto.timingSafeEqual(a, b)` and `return false`, change the first line of the function body to `return true;` and remove/comment out the rest.

Step 2b: Run `npm test`. At minimum one integration test (Stripe/GitHub/Shopify tampered-body or wrong-secret) MUST fail.

Step 2c: Record failing test(s).

Step 2d: `git checkout -- src/crypto/compare.ts`

Step 2e: `git status` — confirm clean.

---

**Mutation 3: Comment out missing-header guard in stripe.ts**

Step 3a: Read `src/providers/stripe.ts`. Find the post-Plan-03 three-way header guard:
```typescript
if (header === undefined) {
  throw new WebhookValidationError({
    reason: 'missing_header',
    provider: 'stripe',
    statusCode: 401,
  });
}
```
Comment out the entire block (add `//` to each line, or wrap in `/* ... */`).

Step 3b: Run `npm test`. The missing-header integration test in `tests/integration/stripe.test.ts` (sends no `stripe-signature` header, expects 401) MUST fail.

Step 3c: Record failing test(s).

Step 3d: `git checkout -- src/providers/stripe.ts`

Step 3e: `git status` — confirm clean.

---

**Mutation 4: Comment out age/tolerance check in stripe.ts**

Step 4a: Read `src/providers/stripe.ts`. Find the timestamp check:
```typescript
if (age > toleranceSeconds) {
  throw new WebhookValidationError({
    reason: 'timestamp_too_old',
    provider: 'stripe',
    statusCode: 401,
  });
}
```
Comment out the entire block.

Step 4b: Run `npm test`. The replay integration test in `tests/integration/stripe.test.ts` (sends oldTimestamp - 301, expects 401) MUST fail.

Step 4c: Record failing test(s).

Step 4d: `git checkout -- src/providers/stripe.ts`

Step 4e: `git status` — confirm clean.

---

**Mutation 5: Swap base64 → hex in shopify.ts**

Step 5a: Read `src/providers/shopify.ts`. Find the line:
```typescript
Buffer.from(header, 'base64')
```
Change to:
```typescript
Buffer.from(header, 'hex')
```

Step 5b: Run `npm test`. The valid-signature integration test in `tests/integration/shopify.test.ts` (sends correct base64 signature, expects 200) MUST fail — because the provider now decodes the base64 header as hex, producing garbage bytes that don't match the computed digest.

Step 5c: Record failing test(s).

Step 5d: `git checkout -- src/providers/shopify.ts`

Step 5e: `git status` — confirm clean.

---

**Final verification:**
After all 5 mutations, run `git status` one final time. MUST report no modifications.
Run `npm test` one final time. MUST pass (green baseline restored).
  </action>
  <verify>
    <automated>git -C /home/taylor_david_ray/src/github.com/cornjacket/ai-sandbox/gsd-walkthru status --short 2>&1 | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - All 5 mutations completed in sequence with no commits
    - After each revert, `git status` reported no modifications to that file
    - Final `git status` reports clean working tree (0 modified files)
    - Final `npm test` passes (green — all tests restored)
    - Failing test names captured for each of the 5 mutations
  </acceptance_criteria>
  <done>5 mutations executed sequentially; each failed an integration test; all source files reverted; tree clean</done>
</task>

<task type="auto">
  <name>Task 3: Write 06-VERIFICATION.md with Guard Removal Demonstrations table</name>
  <files>.planning/phases/06-integration-tests-coverage-gate-negative-case-audit/06-VERIFICATION.md</files>
  <read_first>
    - (No prior file to read — this is a new file)
    - .planning/ROADMAP.md (Phase 6 Success Criteria SC1-SC4 — these are the headings to verify)
    - .planning/phases/06-integration-tests-coverage-gate-negative-case-audit/06-CONTEXT.md (D-18 table shape: guard | mutation (file:line + change) | failed test(s) | reverted (yes/no))
  </read_first>
  <action>
Create `.planning/phases/06-integration-tests-coverage-gate-negative-case-audit/06-VERIFICATION.md` with the following structure. Fill in the `[EVIDENCE FROM TASK 2]` placeholders with the actual failing test names observed during the mutation experiment.

```markdown
# Phase 6 Verification

**Phase:** 6 — Integration Tests, Coverage Gate & Negative-Case Audit
**Verified:** [DATE]
**Status:** PASS / FAIL (fill in after executing)

## SC1 — Supertest Integration Suite (QUAL-02)

- [ ] `tests/integration/stripe.test.ts` exists and all tests pass
- [ ] `tests/integration/github.test.ts` exists and all tests pass
- [ ] `tests/integration/shopify.test.ts` exists and all tests pass
- [ ] Each file has two describe groups: rawBodyCapture mode (BODY-01) and express.json verify-callback mode (BODY-02)
- [ ] `npm test` passes with integration tests included in the run

**Evidence:** [Total test count after integration suite added, e.g., "N tests passing across N files"]

## SC2 — Coverage Gate >90% (QUAL-03)

- [ ] `@vitest/coverage-v8` installed as devDependency
- [ ] `vitest.config.ts` has `provider: 'v8'`, `coverage.include: ['src/crypto/**', 'src/providers/**', 'src/middleware.ts']`, `thresholds.perFile: true` with all metrics at 90
- [ ] `npm run test:coverage` exits 0
- [ ] CI workflow `.github/workflows/test.yml` has coverage step gated to `matrix.node-version == '22.x' && matrix.express-version == '5.x'`
- [ ] `grep -rn "v8 ignore" src/` returns zero matches

**Evidence:** [Output of `npm run test:coverage`, showing all files above 90%]

## SC3 — Negative-Case Suite Fails When Guards Removed (QUAL-04)

See Guard Removal Demonstrations table below.

## SC4 — Holistic QUAL-01 Review

- [ ] D-12 WR-03 (Stripe array-header three-way split): closed in stripe.ts + stripe.test.ts
- [ ] D-13 WR-01 (tolerance NaN/non-finite loud-fail): closed in middleware.ts + middleware.test.ts
- [ ] D-14 WR-05 (strict-numeric t= parser): closed in stripe.ts + stripe.test.ts
- [ ] D-15 P4 WR-02 (outer toThrow on 4 vacuous stripe tests): closed in stripe.test.ts
- [ ] D-15 P4 WR-04 (default-tolerance dedupe): closed in stripe.ts + middleware.ts
- [ ] D-15 P5 WR-01 (empty-string sig tests): closed in github.test.ts + shopify.test.ts
- [ ] D-15 P5 WR-02 (vacuous body-leakage fix): closed in github.test.ts + shopify.test.ts
- [ ] D-16 P5 WR-03 (defense-in-depth leakage assertions): closed in github.test.ts + shopify.test.ts

**Evidence:** `npm test` passes with [N] tests across [N] files — no regressions from audit fixes

## Guard Removal Demonstrations

> **Methodology:** Five sequential manual mutations, each followed by `npm test`. Source reverted with `git checkout -- <file>` after each run. Zero commits during the experiment. Final `git status` clean. Each mutation fails an INTEGRATION test (not a unit test) to demonstrate the integration tier earns its keep.

| # | Guard | Mutation (file + change) | Tier | Failed Integration Test(s) | Reverted |
|---|-------|--------------------------|------|---------------------------|----------|
| 1 | HMAC compare | `src/providers/stripe.ts`: `timingSafeCompare(...)` → `true` (always matches) | integration | [EVIDENCE FROM TASK 2: e.g., "Stripe integration — rawBodyCapture mode > tampered body: returns 401"] | yes |
| 2 | timingSafeCompare | `src/crypto/compare.ts`: function body replaced with `return true` (always true) | integration | [EVIDENCE FROM TASK 2: e.g., "Stripe/GitHub/Shopify tampered-body integration tests"] | yes |
| 3 | missing-header guard (Stripe) | `src/providers/stripe.ts`: `if (header === undefined)` block commented out | integration | [EVIDENCE FROM TASK 2: e.g., "Stripe integration — rawBodyCapture mode > missing Stripe-Signature header: returns 401"] | yes |
| 4 | age/tolerance check | `src/providers/stripe.ts`: `if (age > toleranceSeconds)` block commented out | integration | [EVIDENCE FROM TASK 2: e.g., "Stripe integration — rawBodyCapture mode > replay outside tolerance window: returns 401"] | yes |
| 5 | Shopify base64 decode | `src/providers/shopify.ts`: `Buffer.from(header, 'base64')` → `Buffer.from(header, 'hex')` | integration | [EVIDENCE FROM TASK 2: e.g., "Shopify integration — rawBodyCapture mode > valid base64 signature: returns 200 with webhook.provider === shopify"] | yes |

**Post-experiment state:** `git status` clean — zero uncommitted modifications.
```

Fill in all `[EVIDENCE FROM TASK 2]` cells with the actual failing test names/output from running `npm test` during each mutation. Fill in `[DATE]` with today's date. Fill in test counts. Mark Status as PASS if all SCs are green.
  </action>
  <verify>
    <automated>grep -c "Guard Removal Demonstrations" /home/taylor_david_ray/src/github.com/cornjacket/ai-sandbox/gsd-walkthru/.planning/phases/06-integration-tests-coverage-gate-negative-case-audit/06-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `06-VERIFICATION.md` exists at the correct path
    - File contains `## Guard Removal Demonstrations` section
    - File contains a 5-row markdown table with columns: `#`, `Guard`, `Mutation (file + change)`, `Tier`, `Failed Integration Test(s)`, `Reverted`
    - All 5 table rows have `yes` in the Reverted column
    - All 5 table rows have actual failing test name(s) in the `Failed Integration Test(s)` column (NOT placeholder text)
    - File contains SC1, SC2, SC3, SC4 sections with checkboxes
    - Status is set to PASS if all checks are green
  </acceptance_criteria>
  <done>06-VERIFICATION.md exists with complete SC1-SC4 evidence and a 5-row Guard Removal Demonstrations table with actual failing test names</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mutation experiment → git history | D-19 forbids commits during mutation; only the VERIFICATION.md evidence table enters git |
| VERIFICATION.md evidence | Documents that integration tests (not unit tests) are the failing tier — proving integration coverage earns its keep |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04-01 | Repudiation | Mutation experiment leaving source modified | mitigate | D-19: each mutation is immediately reverted with `git checkout -- <file>`; final `git status` clean check is a hard acceptance criterion; no mutation enters git history |
| T-06-04-02 | Tampering | Mutation failing a unit test instead of an integration test | mitigate | D-17 explicitly requires each of the 5 mutations to fail an INTEGRATION test; the VERIFICATION.md table records the test file path (tests/integration/) to prove tier; if a mutation only fails a unit test, that is a gap to surface |
| T-06-04-03 | Denial of Service | npm test takes too long during mutation runs | accept | Each `npm test` run is ~5-10 seconds; 5 mutations = ~50 seconds total; acceptable for a one-shot verification pass |
| T-06-04-04 | Repudiation | VERIFICATION.md evidence gaps | mitigate | Acceptance criteria require all 5 table rows to have actual test names (not placeholders); reviewer can cross-reference test names against tests/integration/ file contents |
</threat_model>

<verification>
```bash
# 1. VERIFICATION.md exists
ls .planning/phases/06-integration-tests-coverage-gate-negative-case-audit/06-VERIFICATION.md

# 2. Guard Removal Demonstrations section present
grep "Guard Removal Demonstrations" .planning/phases/06-integration-tests-coverage-gate-negative-case-audit/06-VERIFICATION.md

# 3. Table has 5 rows (look for the | # | column entries 1..5)
grep -c "^| [1-5] |" .planning/phases/06-integration-tests-coverage-gate-negative-case-audit/06-VERIFICATION.md

# 4. All reverted yes
grep -c "| yes |" .planning/phases/06-integration-tests-coverage-gate-negative-case-audit/06-VERIFICATION.md

# 5. Git tree is clean after the experiment
git status --short

# 6. Full test suite green (final baseline)
npm test
```
</verification>

<success_criteria>
- `06-VERIFICATION.md` written with all 5 guard mutations evidenced with actual failing integration test names
- All 5 mutations confirmed reverted; `git status` clean
- `npm test` passes (green baseline restored)
- QUAL-04 satisfied: the suite "demonstrably fails when each guard is removed" — covering tampered body, wrong secret, missing header, Stripe replay, Shopify hex-vs-base64 encoding
- ROADMAP SC3 satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/06-integration-tests-coverage-gate-negative-case-audit/06-04-SUMMARY.md`
</output>
