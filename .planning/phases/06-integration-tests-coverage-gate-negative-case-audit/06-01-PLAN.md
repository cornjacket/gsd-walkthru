---
phase: "06"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - vitest.config.ts
  - package.json
  - .github/workflows/test.yml
autonomous: true
requirements:
  - QUAL-03

must_haves:
  truths:
    - "Running `npm run test:coverage` executes all tests AND enforces the >90% gate"
    - "The CI pipeline fails on the Node 22 × Express 5.x cell when coverage drops below 90% on any gated file"
    - "No `/* v8 ignore */` directive exists anywhere in src/"
    - "Coverage is scoped to exactly src/crypto/**, src/providers/**, and src/middleware.ts — not all of src/"
  artifacts:
    - path: "vitest.config.ts"
      provides: "Coverage config with perFile threshold gate"
      contains: "perFile: true"
    - path: "package.json"
      provides: "test:coverage script"
      contains: "test:coverage"
    - path: ".github/workflows/test.yml"
      provides: "CI coverage step gated to Node 22 × Express 5.x"
      contains: "npm run test:coverage"
  key_links:
    - from: "vitest.config.ts"
      to: "CI coverage step"
      via: "npm run test:coverage script"
      pattern: "vitest run --coverage"
    - from: "coverage.include"
      to: "threshold gate"
      via: "vitest coverage.thresholds.perFile"
      pattern: "perFile.*true"
---

<objective>
Install `@vitest/coverage-v8`, configure the coverage gate in `vitest.config.ts`, add the `test:coverage` npm script, and add the coverage CI step gated to the canonical Node 22 × Express 5.x matrix cell.

Purpose: Satisfies QUAL-03 — enforced >90% per-file line/branch/function/statement coverage on `src/crypto/**`, `src/providers/**`, and `src/middleware.ts` in CI. This is purely tooling wiring; no source or test files are touched.

Output: `vitest.config.ts` with coverage block, updated `package.json`, updated `.github/workflows/test.yml`.
</objective>

<execution_context>
@/home/taylor_david_ray/src/github.com/cornjacket/ai-sandbox/gsd-walkthru/.claude/get-shit-done/workflows/execute-plan.md
@/home/taylor_david_ray/src/github.com/cornjacket/ai-sandbox/gsd-walkthru/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/taylor_david_ray/src/github.com/cornjacket/ai-sandbox/gsd-walkthru/.planning/PROJECT.md
@/home/taylor_david_ray/src/github.com/cornjacket/ai-sandbox/gsd-walkthru/.planning/ROADMAP.md
@/home/taylor_david_ray/src/github.com/cornjacket/ai-sandbox/gsd-walkthru/.planning/STATE.md

<interfaces>
<!-- Current vitest.config.ts (full file — 8 lines): -->
```typescript
import { defineConfig } from 'vitest/config';

export default defineConfig({
  test: {
    include: ['src/**/*.test.ts'],
    environment: 'node',
  },
});
```

<!-- Current package.json scripts section: -->
```json
"scripts": {
  "build": "tsup",
  "test": "vitest run",
  "lint": "biome check .",
  "format": "biome format --write .",
  "typecheck": "tsc --noEmit -p tsconfig.test.json"
}
```

<!-- Current package.json devDependencies: -->
```json
"devDependencies": {
  "@biomejs/biome": "^2.4.14",
  "@types/express": "^4.17.21",
  "@types/node": "^20.0.0",
  "@types/supertest": "^7.2.0",
  "supertest": "^7.2.2",
  "tsup": "^8.5.1",
  "typescript": "^6.0.3",
  "vitest": "^4.1.5"
}
```

<!-- Current .github/workflows/test.yml steps section (last 10 lines): -->
```yaml
      - name: Lint (Biome check)
        run: npm run lint

      - name: Build (tsup)
        run: npm run build

      - name: Test (Vitest)
        run: npm test
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install @vitest/coverage-v8 and add test:coverage script</name>
  <files>package.json</files>
  <read_first>
    - package.json (MUST read first — need exact current content to preserve all fields)
  </read_first>
  <action>
Run `npm install --save-dev @vitest/coverage-v8` from the repo root. This adds the package and updates package.json devDependencies automatically.

Then edit `package.json` scripts to add `"test:coverage": "vitest run --coverage"` after the existing `"test": "vitest run"` line. The final scripts block must be:

```json
"scripts": {
  "build": "tsup",
  "test": "vitest run",
  "test:coverage": "vitest run --coverage",
  "lint": "biome check .",
  "format": "biome format --write .",
  "typecheck": "tsc --noEmit -p tsconfig.test.json"
}
```

Do NOT alter any other field in package.json.
  </action>
  <verify>
    <automated>grep -c '"test:coverage"' /home/taylor_david_ray/src/github.com/cornjacket/ai-sandbox/gsd-walkthru/package.json</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` contains `"test:coverage": "vitest run --coverage"`
    - `package.json` devDependencies contains `"@vitest/coverage-v8"`
    - `node_modules/@vitest/coverage-v8` directory exists after npm install
    - All existing scripts (`build`, `test`, `lint`, `format`, `typecheck`) remain present unchanged
  </acceptance_criteria>
  <done>package.json has the test:coverage script and @vitest/coverage-v8 devDependency installed</done>
</task>

<task type="auto">
  <name>Task 2: Add coverage block to vitest.config.ts and expand test include</name>
  <files>vitest.config.ts</files>
  <read_first>
    - vitest.config.ts (MUST read first — the full current file is 8 lines; replace completely)
    - .planning/phases/06-integration-tests-coverage-gate-negative-case-audit/06-CONTEXT.md (D-01..D-06 decisions)
  </read_first>
  <action>
Replace the entire contents of `vitest.config.ts` with the following (per D-01, D-02, D-03, D-05, D-06):

```typescript
import { defineConfig } from 'vitest/config';

export default defineConfig({
  test: {
    include: ['src/**/*.test.ts', 'tests/integration/**/*.test.ts'],
    environment: 'node',
  },
  coverage: {
    provider: 'v8',
    include: ['src/crypto/**', 'src/providers/**', 'src/middleware.ts'],
    thresholds: {
      perFile: true,
      statements: 90,
      branches: 90,
      functions: 90,
      lines: 90,
    },
  },
});
```

Key constraints (do NOT deviate):
- D-01: `provider: 'v8'` — NOT istanbul.
- D-02: `coverage.include` is EXACTLY `['src/crypto/**', 'src/providers/**', 'src/middleware.ts']`. Do NOT expand to all of `src/**`.
- D-03: `thresholds.perFile: true` with all four metrics at 90. Single source of truth here — no external script.
- D-05: Zero `/* v8 ignore */` directives anywhere in `src/`. Do NOT add any.
- D-06: `test.include` now has BOTH `'src/**/*.test.ts'` AND `'tests/integration/**/*.test.ts'`.
  </action>
  <verify>
    <automated>grep -c "perFile: true" /home/taylor_david_ray/src/github.com/cornjacket/ai-sandbox/gsd-walkthru/vitest.config.ts</automated>
  </verify>
  <acceptance_criteria>
    - `vitest.config.ts` contains `provider: 'v8'`
    - `vitest.config.ts` contains `'tests/integration/**/*.test.ts'` in test.include
    - `vitest.config.ts` contains `perFile: true`
    - `vitest.config.ts` coverage.include contains exactly `'src/crypto/**'`, `'src/providers/**'`, `'src/middleware.ts'` (grep each)
    - `vitest.config.ts` does NOT contain `istanbul`
    - `grep -rn "v8 ignore" src/` returns zero matches
  </acceptance_criteria>
  <done>vitest.config.ts has coverage block with per-file >90% gate, V8 provider, and integration test include expanded</done>
</task>

<task type="auto">
  <name>Task 3: Add coverage CI step gated to Node 22 × Express 5.x</name>
  <files>.github/workflows/test.yml</files>
  <read_first>
    - .github/workflows/test.yml (MUST read first — need exact current YAML to preserve indentation and all steps)
    - .planning/phases/06-integration-tests-coverage-gate-negative-case-audit/06-CONTEXT.md (D-04 decision)
  </read_first>
  <action>
Append a new step AFTER the existing `- name: Test (Vitest)` step (which runs `npm test`). The new step runs ONLY on the canonical coverage cell (Node 22 × Express 5.x) per D-04. Use an `if:` conditional.

The new step to append (maintain 6-space indentation to match existing steps):

```yaml
      - name: Test Coverage (Node 22 × Express 5.x)
        if: matrix.node-version == '22.x' && matrix.express-version == '5.x'
        run: npm run test:coverage
```

Key constraints (per D-04):
- The `if:` conditional is MANDATORY — this step must NOT run on all 6 matrix cells, only on the one Node 22 × Express 5.x cell.
- Do NOT create a separate `coverage.yml` workflow file.
- Do NOT remove or modify any existing steps.
- The step name must clearly indicate which matrix cell it gates on.
  </action>
  <verify>
    <automated>grep -c "npm run test:coverage" /home/taylor_david_ray/src/github.com/cornjacket/ai-sandbox/gsd-walkthru/.github/workflows/test.yml</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/test.yml` contains `npm run test:coverage`
    - `.github/workflows/test.yml` contains `matrix.node-version == '22.x' && matrix.express-version == '5.x'` (the if conditional)
    - The coverage step appears AFTER the `Test (Vitest)` step
    - All 5 existing steps (Checkout, Set up Node.js, Install dependencies, Install Express, Lint, Build, Test) remain present unchanged
  </acceptance_criteria>
  <done>CI workflow has a coverage step that only runs on Node 22 × Express 5.x, placed after the test step</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI → coverage report | Coverage thresholds enforce a pass/fail gate; threshold values in vitest.config.ts are the single source of truth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-01 | Tampering | vitest.config.ts coverage.include | mitigate | The `perFile: true` threshold ensures every included file must independently meet the gate — no single low-coverage file can be "averaged away" by high-coverage peers |
| T-06-01-02 | Repudiation | `/* v8 ignore */` escape hatch | mitigate | D-05 forbids all v8 ignore directives in `src/`; acceptance criteria verifies zero matches with `grep -rn "v8 ignore" src/`; this closes the path where a developer hides uncovered branches |
| T-06-01-03 | Elevation of Privilege | Coverage step on all 6 CI cells | accept | Running only on Node 22 × Express 5.x by design (D-04); V8 coverage is Node-version-agnostic for this codebase; the 5 other cells remain test-only with no coverage overhead |
| T-06-01-04 | Denial of Service | Coverage gate tripping on legitimate code | accept | Gate set at 90% with headroom; Phases 2-5 co-located tests cover crypto/provider logic; if gate trips, fix is a real test (D-05), not an ignore comment |
</threat_model>

<verification>
```bash
# 1. Confirm @vitest/coverage-v8 installed
ls node_modules/@vitest/coverage-v8/package.json

# 2. Confirm test:coverage script present
grep '"test:coverage"' package.json

# 3. Confirm vitest.config.ts has perFile gate
grep "perFile: true" vitest.config.ts

# 4. Confirm integration tests included
grep "tests/integration" vitest.config.ts

# 5. Confirm coverage.include is exactly the three gated paths
grep "src/crypto" vitest.config.ts && grep "src/providers" vitest.config.ts && grep "src/middleware.ts" vitest.config.ts

# 6. Confirm no v8 ignore directives in src/
grep -rn "v8 ignore" src/ | wc -l  # Must be 0

# 7. Confirm CI coverage step with conditional
grep "npm run test:coverage" .github/workflows/test.yml
grep "matrix.node-version == '22.x'" .github/workflows/test.yml
```
</verification>

<success_criteria>
- `npm run test:coverage` runs and exits 0 (with Phase 2-5 test suites covering >90% of gated files)
- `vitest.config.ts` coverage.include is EXACTLY `['src/crypto/**', 'src/providers/**', 'src/middleware.ts']`
- `vitest.config.ts` has `thresholds.perFile: true` with all four metrics at 90
- `.github/workflows/test.yml` has coverage step gated by `matrix.node-version == '22.x' && matrix.express-version == '5.x'`
- `grep -rn "v8 ignore" src/` returns zero matches
- QUAL-03 satisfied: >90% per-file coverage gate enforced in CI on core validation logic
</success_criteria>

<output>
After completion, create `.planning/phases/06-integration-tests-coverage-gate-negative-case-audit/06-01-SUMMARY.md`
</output>
