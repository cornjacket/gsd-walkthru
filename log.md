# Work log

Task-granularity record of work in this repo, indexed by short commit hash. See `CLAUDE.md` → "Work log (log.md)" for the rule. Each entry is one line:

```
- **YYYY-MM-DD** — <one or two sentences of what changed and why>. Task: `<task-name>`. [Subtask: `<subtask-name>`.] Commit: `<short-hash>`.
```

Newest entries at the bottom.

- **2026-05-06** — Planned Phase 3 (Body Handling & Public API Surface) end-to-end via `/gsd-plan-phase 3`: research → validation strategy → patterns map → 7 PLAN.md files in 5 waves → revision pass after plan-checker found 1 blocker + 4 warnings → re-verified clean. All 5 phase requirements (BODY-01, BODY-02, API-01, API-03, API-04) covered. Task: `phase-3-plan`. Commit: `61e69b6`.
- **2026-05-07** — Added `tsconfig.test.json` (extends base config, drops `**/*.test.ts` exclusion) and `typecheck` npm script so tsc includes test files in its compilation graph (D-20/RESEARCH Pitfall 1); installed supertest and @types/supertest as devDependencies to enable Phase 3 integration-shape tests (D-21). Task: `03-01`. Commit: `c0b38c8`.
- **2026-05-07** — Shipped provider plumbing for Phase 3: Provider interface (D-01), Map-backed registry with last-write-wins semantics and _clearRegistryForTesting, 3 discriminated-union branch types co-located with self-registering stubs (StripeWebhook/GitHubWebhook/ShopifyWebhook), and registry tests (3 cases, Pitfall 2 isolation). Task: `03-02-provider-interface-registry-stubs`. Commit: `f1ec0bf`.
- **2026-05-07** — Implemented both raw-body capture surfaces (`rawBodyCapture()` standalone middleware and `captureRawBody` verify-callback helper) under `src/raw-body/` with co-located supertest-driven tests; 8 new tests pass covering capture, 413 limit enforcement, skip-if-set guard, cross-surface parity, and no-console contract. Task: `03-03-raw-body-capture-surfaces`. Commit: `602a590`.
- **2026-05-07** — Widened `WebhookValidationReason` union with `'malformed_payload'` (Phase 3 D-17); added messageFor derivation test and leakage test extension per D-07 same-commit pattern. Task: `03-04 Task 1: Widen WebhookValidationReason with 'malformed_payload'`. Commit: `2118668`.
- **2026-05-07** — Created `webhookErrorHandler()` opt-in 4-arg Express error middleware factory (D-14/D-15/D-16) with 9 supertest-driven tests covering all 5 reasons, passthrough, no-logging, fn.length, and response no-leakage. Task: `03-04 Task 2: webhookErrorHandler() factory + tests`. Commit: `0726f82`.
- **2026-05-07** — Shipped `src/types.ts` (WebhookMetadata discriminated union + global Express.Request augmentation via declaration merging) and `src/types.test.ts` (6 it blocks, 5 @ts-expect-error cross-branch directives proving SC3); `npm run typecheck` exits 0 structurally proving that accessing a Shopify-only field on a Stripe-narrowed branch is a compile-time error. Task: `03-05`. Commit: `286143b`.
- **2026-05-07** — Shipped `src/middleware.ts` (createWebhookMiddleware factory: sync throws on unknown provider + empty secret, malformed_payload on missing rawBody, next(err) delegation, req.webhook assignment on success; provider-agnostic, zero console.*) and `src/middleware.test.ts` (8 it blocks: D-02/D-04/D-07/D-13/D-16/API-04 all covered via fake provider per D-22, Pitfall 2 isolation); full suite 57 tests green. Task: `03-06`. Commit: `3e00efb`.
- **2026-05-07** — Finalized `src/index.ts` public barrel with full Phase 3 surface (D-19): all 5 Phase 2 exports preserved + 9 Phase 3 named re-exports + 3 D-03 side-effect imports registering providers transparently; extended `src/index.test.ts` to 15 it blocks (5 Phase 2 + 10 Phase 3 including D-03 registration smoke); full suite 67 tests green, build clean, dist/index.d.ts declares all Phase 3 symbols. Task: `03-07`. Commit: `0866a0b`.
- **2026-05-07** — Executed Phase 3 end-to-end via `/gsd-execute-phase 3`: 4 waves of worktree-isolated executor agents (7 plans), per-wave merge with manual log.md union resolution across three worktrees, post-merge build/typecheck/test gates green at every wave, code review (1 blocker + 6 warnings, advisory), goal-backward verifier passed 5/5 must-haves, ROADMAP/STATE/PROJECT.md updated. Final state: 67 tests green across 10 files. Task: `phase-3-execute`. Commit: `f6c9fa4`.
- **2026-05-07** — Rebased local main onto origin/main (`--rebase-merges`) to integrate the two ai-project-status bootstrap commits (work-log rule + daily-plan SessionStart hook); resolved 4 log.md union conflicts and 1 package.json `@types/supertest` version conflict; renamed local `.claude/settings.json` → `.claude/settings.local.json` and gitignored the local override so origin's shared SessionStart config and the GSD per-user hooks coexist; back-filled all stale commit hashes in log.md and 4 03-* SUMMARY.md files post-rebase; wrote `daily-plan.md` for 2026-05-07 (push origin → fix BL-01 → /gsd-code-review 3 --fix → /gsd-discuss-phase 4). Task: `rebase-and-daily-plan`. Commit: `fd4631e`.
- **2026-05-07** — Closed Phase 3 review findings: declared `raw-body ^3.0.0` as a runtime dep (BL-01 ship-blocker — was resolving only via express's transitive copy, fragile under pnpm/Yarn PnP), exported `listProviders()` from registry and replaced the hard-coded provider list in the unknown-provider error message (WR-02), tightened the secret guard to reject whitespace-only values (WR-03), and widened `parsed` to `unknown` across all three provider branches (WR-05); deferred WR-01/WR-04/WR-06 into Phase 4 context. 68/68 tests green (+1 for whitespace-secret guard). Rode 03-06/03-07 SUMMARY hash back-fills in this commit per CLAUDE.md Rule 3. Task: `phase-3-review-fixes`. Commit: `a99bbe7`.
- **2026-05-07** — Refreshed planning state after Phase 3 close: STATE.md frontmatter (completed_plans 7→14, status/stopped_at/last_activity rewritten, current focus → Phase 4) and body (velocity, session continuity); flipped ROADMAP.md Phase 3 line `[ ]` → `[x]` (was inconsistent with completed_phases:3 in STATE.md and with the per-plan checkboxes 03-01..03-07 already marked done). Surfaced ROADMAP/STATE/REQUIREMENTS/PROJECT checkbox-duplication concern in conversation. Task: `state-refresh-post-phase-3`. Commit: `2269eea`.
- **2026-05-07** — Diagnosed and fixed UTC-vs-Pacific off-by-one in `daily-plan.md` dating: Claude Code sandbox runs UTC with no `TZ` set, so `date.today()` after ~17:00 PDT returns the next calendar day. Combined with CLAUDE.md Rule 4 ("at signoff, write tomorrow's plan"), evening sessions wrote plans dated *two days* ahead of the user's reality. Fix: added `"env": {"TZ": "America/Los_Angeles"}` to `~/.claude/settings.json` (user-global, takes effect next session). Corrected stale `2026-05-08` date stamps in `daily-plan.md` header, `.planning/STATE.md` (4 lines), and `log.md` (2 entries + 1 inline reference). Confirmed via `RemoteTrigger get` that the `/schedule` routine fires `13 12 * * *` UTC = 5:13 AM PDT daily, so forward-write convention stays. Task: `fix-tz-daily-plan-dates`. Commit: `_pending_`.
