# Work log

Task-granularity record of work in this repo, indexed by short commit hash. See `CLAUDE.md` → "Work log (log.md)" for the rule. Each entry is one line:

```
- **YYYY-MM-DD** — <one or two sentences of what changed and why>. Task: `<task-name>`. [Subtask: `<subtask-name>`.] Commit: `<short-hash>`.
```

Newest entries at the bottom.

- **2026-05-06** — Planned Phase 3 (Body Handling & Public API Surface) end-to-end via `/gsd-plan-phase 3`: research → validation strategy → patterns map → 7 PLAN.md files in 5 waves → revision pass after plan-checker found 1 blocker + 4 warnings → re-verified clean. All 5 phase requirements (BODY-01, BODY-02, API-01, API-03, API-04) covered. Task: `phase-3-plan`. Commit: `61e69b6`.
- **2026-05-07** — Added `tsconfig.test.json` (extends base config, drops `**/*.test.ts` exclusion) and `typecheck` npm script so tsc includes test files in its compilation graph (D-20/RESEARCH Pitfall 1); installed supertest and @types/supertest as devDependencies to enable Phase 3 integration-shape tests (D-21). Task: `03-01`. Commit: `c0b38c8`.
- **2026-05-07** — Shipped provider plumbing for Phase 3: Provider interface (D-01), Map-backed registry with last-write-wins semantics and _clearRegistryForTesting, 3 discriminated-union branch types co-located with self-registering stubs (StripeWebhook/GitHubWebhook/ShopifyWebhook), and registry tests (3 cases, Pitfall 2 isolation). Task: `03-02-provider-interface-registry-stubs`. Commit: `f1ec0bf`.
- **2026-05-07** — Implemented both raw-body capture surfaces (`rawBodyCapture()` standalone middleware and `captureRawBody` verify-callback helper) under `src/raw-body/` with co-located supertest-driven tests; 8 new tests pass covering capture, 413 limit enforcement, skip-if-set guard, cross-surface parity, and no-console contract. Task: `03-03-raw-body-capture-surfaces`. Commit: `602a590`.
- **2026-05-07** — Widened `WebhookValidationReason` union with `'malformed_payload'` (Phase 3 D-17); added messageFor derivation test and leakage test extension per D-07 same-commit pattern. Task: `03-04 Task 1: Widen WebhookValidationReason with 'malformed_payload'`. Commit: `2118668`.
- **2026-05-07** — Created `webhookErrorHandler()` opt-in 4-arg Express error middleware factory (D-14/D-15/D-16) with 9 supertest-driven tests covering all 5 reasons, passthrough, no-logging, fn.length, and response no-leakage. Task: `03-04 Task 2: webhookErrorHandler() factory + tests`. Commit: `0726f82`.
- **2026-05-07** — Shipped `src/types.ts` (WebhookMetadata discriminated union + global Express.Request augmentation via declaration merging) and `src/types.test.ts` (6 it blocks, 5 @ts-expect-error cross-branch directives proving SC3); `npm run typecheck` exits 0 structurally proving that accessing a Shopify-only field on a Stripe-narrowed branch is a compile-time error. Task: `03-05`. Commit: `286143b`.
- **2026-05-07** — Shipped `src/middleware.ts` (createWebhookMiddleware factory: sync throws on unknown provider + empty secret, malformed_payload on missing rawBody, next(err) delegation, req.webhook assignment on success; provider-agnostic, zero console.*) and `src/middleware.test.ts` (8 it blocks: D-02/D-04/D-07/D-13/D-16/API-04 all covered via fake provider per D-22, Pitfall 2 isolation); full suite 57 tests green. Task: `03-06`. Commit: `3e00efb`.
- **2026-05-07** — Finalized `src/index.ts` public barrel with full Phase 3 surface (D-19): all 5 Phase 2 exports preserved + 9 Phase 3 named re-exports + 3 D-03 side-effect imports registering providers transparently; extended `src/index.test.ts` to 15 it blocks (5 Phase 2 + 10 Phase 3 including D-03 registration smoke); full suite 67 tests green, build clean, dist/index.d.ts declares all Phase 3 symbols. Task: `03-07`. Commit: `0866a0b`.
- **2026-05-07** — Executed Phase 3 end-to-end via `/gsd-execute-phase 3`: 4 waves of worktree-isolated executor agents (7 plans), per-wave merge with manual log.md union resolution across three worktrees, post-merge build/typecheck/test gates green at every wave, code review (1 blocker + 6 warnings, advisory), goal-backward verifier passed 5/5 must-haves, ROADMAP/STATE/PROJECT.md updated. Final state: 67 tests green across 10 files. Task: `phase-3-execute`. Commit: `f6c9fa4`.
- **2026-05-07** — Rebased local main onto origin/main (`--rebase-merges`) to integrate the two ai-project-status bootstrap commits (work-log rule + daily-plan SessionStart hook); resolved 4 log.md union conflicts and 1 package.json `@types/supertest` version conflict; renamed local `.claude/settings.json` → `.claude/settings.local.json` and gitignored the local override so origin's shared SessionStart config and the GSD per-user hooks coexist; back-filled all stale commit hashes in log.md and 4 03-* SUMMARY.md files post-rebase; wrote `daily-plan.md` for 2026-05-07 (push origin → fix BL-01 → /gsd-code-review 3 --fix → /gsd-discuss-phase 4). Task: `rebase-and-daily-plan`. Commit: `fd4631e`.
- **2026-05-07** — Closed Phase 3 review findings: declared `raw-body ^3.0.0` as a runtime dep (BL-01 ship-blocker — was resolving only via express's transitive copy, fragile under pnpm/Yarn PnP), exported `listProviders()` from registry and replaced the hard-coded provider list in the unknown-provider error message (WR-02), tightened the secret guard to reject whitespace-only values (WR-03), and widened `parsed` to `unknown` across all three provider branches (WR-05); deferred WR-01/WR-04/WR-06 into Phase 4 context. 68/68 tests green (+1 for whitespace-secret guard). Rode 03-06/03-07 SUMMARY hash back-fills in this commit per CLAUDE.md Rule 3. Task: `phase-3-review-fixes`. Commit: `a99bbe7`.
- **2026-05-07** — Refreshed planning state after Phase 3 close: STATE.md frontmatter (completed_plans 7→14, status/stopped_at/last_activity rewritten, current focus → Phase 4) and body (velocity, session continuity); flipped ROADMAP.md Phase 3 line `[ ]` → `[x]` (was inconsistent with completed_phases:3 in STATE.md and with the per-plan checkboxes 03-01..03-07 already marked done). Surfaced ROADMAP/STATE/REQUIREMENTS/PROJECT checkbox-duplication concern in conversation. Task: `state-refresh-post-phase-3`. Commit: `2269eea`.
- **2026-05-07** — Diagnosed and fixed UTC-vs-Pacific off-by-one in `daily-plan.md` dating: Claude Code sandbox runs UTC with no `TZ` set, so `date.today()` after ~17:00 PDT returns the next calendar day. Combined with CLAUDE.md Rule 4 ("at signoff, write tomorrow's plan"), evening sessions wrote plans dated *two days* ahead of the user's reality. Fix: added `"env": {"TZ": "America/Los_Angeles"}` to `~/.claude/settings.json` (user-global, takes effect next session). Corrected stale `2026-05-08` date stamps in `daily-plan.md` header, `.planning/STATE.md` (4 lines), and `log.md` (2 entries + 1 inline reference). Confirmed via `RemoteTrigger get` that the `/schedule` routine fires `13 12 * * *` UTC = 5:13 AM PDT daily, so forward-write convention stays. Task: `fix-tz-daily-plan-dates`. Commit: `0ac66f1`.
- **2026-05-07** — Wrote initial `README.md` (was empty): project description with v0.0.1 in-development status note, per-provider notes (Stripe `<t>.<body>` + timestamp window, GitHub SHA-256 only / `X-GitHub-Delivery` for consumer dedup, Shopify base64-not-hex gotcha), system-level data flow ASCII diagram (sender → public internet → trust boundary → app), internal data flow ASCII diagram (rawBodyCapture → createWebhookMiddleware → Provider.validate / next(err) → req.webhook / webhookErrorHandler), three structural contracts (no res writes, structurally-sealed error class, library-never-logs), and a quick example. Task: `readme-initial-draft`. Commit: `628867d`.
- **2026-05-07** — Expanded `HMAC` → `Hash-based Message Authentication Code (HMAC)` at first occurrence in README tagline (per-user request); subsequent uses stay as `HMAC`. Task: `readme-hmac-expansion`. Commit: `fa4e9f3`.
- **2026-05-07** — Researched Phase 4: Stripe Provider via `/gsd-research-phase 4`. Created `.planning/phases/04-stripe-provider/04-RESEARCH.md` with Stripe-Signature header format (t=timestamp,v1=hex), HMAC-SHA256 input exactly `${t}.${rawBody.toString('utf8')}`, constant-time multi-segment comparison, past-only tolerance window (300s default, configurable), JSON-parse-after-signature order, and new `'invalid_signature_format'` reason. Verified via official Stripe docs, stripe-node examples, and Phase 2/3 locked contracts. All STRP-01/STRP-02/STRP-03 requirements mapped to test cases. Confidence: HIGH. Task: `phase-4-research`. Commit: `1315b19`.
- **2026-05-07** — Planned Phase 4 (Stripe Provider) via `/gsd-plan-phase 4`: created 3 PLAN.md files in 2 waves (Wave 1: 04-01 error-widening + 04-02 middleware tolerance; Wave 2: 04-03 stripe implementation + tests), filled 04-VALIDATION.md scaffold (15 test cases mapped, wave_0_complete: true), updated ROADMAP.md with plan list; all 3 requirements STRP-01/STRP-02/STRP-03 covered. Back-filled `phase-4-research` hash in this commit (CLAUDE.md Rule 3). Task: `phase-4-plan`. Commit: `1315b19`.
- **2026-05-07** — Closed `/gsd-plan-phase 4` orchestrator: gsd-plan-checker returned VERIFICATION PASSED across 13 dimensions on first iteration (no revision loop needed); committed the previously-untracked 04-RESEARCH.md (1100+ lines) and 04-PATTERNS.md (5 files mapped to Phase 2/3 analogs) so the plan artifacts are version-controlled before /gsd-execute-phase 4. Rode the readme-hmac-expansion `_pending_` → `fa4e9f3` back-fill into this commit per CLAUDE.md Rule 3. Task: `phase-4-plan-orchestrator-close`. Commit: `a9e76df`.
- **2026-05-07** — Widened `WebhookValidationReason` union with `'invalid_signature_format'` (Phase 4 D-05) in `src/errors.ts`; extended `errors.test.ts` with round-trip and message derivation tests; extended `src/index.test.ts` with D-14 reachability smoke; TDD RED gate confirmed via `npm run typecheck` (4 TypeScript errors), GREEN via implementation; full suite 71 tests green (no regressions). Task: `04-01`. Commit: `81b1a26`.
- **2026-05-07** — Added `tolerance?: number` to `CreateWebhookMiddlewareOptions` in `src/middleware.ts` (default 300 s, JSDoc documented) and resolved `const tolerance = options.tolerance ?? 300` in the factory closure; tolerance is passed to `provider.validate` as an optional 3rd argument via a narrow Function cast (D-16: Provider interface unchanged) so the Stripe provider can enforce its timestamp window (STRP-02). All 68 tests green, DTS build clean. Task: `04-02`. Commit: `65e47e9`.
- **2026-05-07** — Replaced Phase 3 stub in `src/providers/stripe.ts` with real 9-step HMAC-SHA256 validator (D-08 order: rawBody guard → header check → parse → HMAC → timing-safe compare all v1= segments → timestamp → JSON.parse → return); exported `stripeProvider` for direct unit testing; TypeScript strict build clean. Created `src/providers/stripe.test.ts` with 15 unit tests covering all D-12 SC4-binding cases (happy path, tampered body, replay/configurable-tolerance, multi-v1 rotation) and D-13 negative cases (missing_header, invalid_signature_format, malformed_payload, future timestamp, no-id/numeric-id, leakage assertion); full suite 86/86 green; STRP-01/STRP-02/STRP-03 satisfied. Task: `04-03`. Commit: `0d2eaf7`.
- **2026-05-07** — Executed Phase 4 end-to-end via `/gsd-execute-phase 4`: 2 waves of worktree-isolated executor agents (3 plans), Wave 1 parallel (04-01 + 04-02) with manual log.md union resolution, Wave 2 sequential (04-03); per-wave post-merge build/typecheck/test gates green at every wave; gsd-code-review surfaced 5 advisory warnings + 4 info (0 blockers — all 5 core security focus areas pass); gsd-verifier confirmed 4/4 SCs verified, 3/3 requirements covered, 17/17 observable truths and all 5 security-critical paths verified, but flagged `status: gaps_found` because it conflated the advisory code-review warnings with phase gaps; user directed treating verifier as passed. Aligned ROADMAP top-level Phase 4 `[ ]` → `[x]` and Progress table (3+4 marked Complete) since `phase.complete` only flipped per-plan checkboxes when verifier returned `gaps_found` — same write-skip behavior surfaced after Phase 3 close in commit `2269eea`. Evolved PROJECT.md (Stripe replay-protection requirement promoted Active → Validated, Key Decision row updated, Last-updated footer rewritten). Final state: 86 tests green across 11 files, 0 critical findings, 5 advisory warnings tracked in 04-REVIEW.md for follow-up. Task: `phase-4-execute`. Commit: `cb09563`.
- **2026-05-07** — Wrote `daily-plan.md` for 2026-05-08: full Phase 5 (GitHub & Shopify Providers) arc in one sitting (`/gsd-discuss-phase 5` → `/gsd-plan-phase 5` → `/gsd-execute-phase 5`), mirroring the Phase 4 pattern; surfaced GHUB-01 SHA-1 deprecated-header explicit reject and SHOP-01 base64-vs-hex as the two failure modes that need to lock down in discuss before plan revisions, and noted Phase 3 D-11 metadata pass-through (`deliveryId`, `topic`, `webhookId`) plus the 5-advisory carry-over from `04-REVIEW.md`. Task: `daily-plan-2026-05-08`. Commit: `6c7241c`.
- **2026-05-08** — Discussed Phase 5 (GitHub & Shopify Providers) via `/gsd-discuss-phase 5`: created `.planning/phases/05-github-shopify-providers/05-CONTEXT.md` + `05-DISCUSSION-LOG.md` with 13 decisions across 4 user-selected gray areas (GitHub SHA-1 rejection, Shopify hex-vs-base64, metadata header robustness, eventId/timestamp/parsed semantics). Key locks: NO new reasons in Phase 5 (final union stays at 6 from Phase 4) — GitHub SHA-1-only → `'missing_header'` (deprecated header invisible to validator), Shopify hex-as-base64 → `'signature_mismatch'` via P2 D-09 length-mismatch path, metadata accept-and-empty (mirror P4 D-09 for body.id), auth-header WR-03 fix from day one in Phase 5 (Phase 4 stripe.ts deferred to Phase 6 audit), `eventId === deliveryId` (GH) / `webhookId` (SH), `timestamp = Math.floor(Date.now()/1000)` at validation time (honest receipt-time, no provider-signed timestamp); JSON.parse-after-signature mirrors P4 D-08 step order; modified-files scope limited to `src/providers/{github,shopify}.{ts,test.ts}`. Task: `phase-5-discuss`. Commit: `bf3900b`.
- **2026-05-08** — Planned Phase 5 (GitHub & Shopify Providers) end-to-end via `/gsd-plan-phase 5`: research → pattern map → 2 PLAN.md files in 1 wave (fully parallel, no cross-dep) → plan-checker VERIFICATION PASSED first iteration with 2 non-blocking warnings (research_resolution convention consistent with Phase 4; one regex acceptance literal with clarifying parenthetical). 05-01 covers GHUB-01/GHUB-02/GHUB-03 → SC1/SC2/SC5; 05-02 covers SHOP-01/SHOP-02 → SC3/SC4/SC5. Both plans modify only their per-provider files (`src/providers/{github,shopify}.{ts,test.ts}`); D-10 three-way header split (WR-03 fix) and D-14 outer-toThrow guards (WR-02 fix) shipped from day one; D-07 Buffer-direct HMAC (no UTF-8 round-trip) is the planned divergence from Phase 4 stripe.ts. VALIDATION.md filled in: 4-row Per-Task Verification Map, no Wave 0 scaffolding (co-located test pattern), `nyquist_compliant: true`. Coverage: 5/5 REQ-IDs in plan frontmatter; 17/17 D-XX decisions referenced. ROADMAP Phase 5 plan list updated; STATE.md → `ready_to_execute`, total_plans 17 → 19. Task: `phase-5-plan`. Commit: `6c9908a`.
- **2026-05-08** — Executed Phase 5 plan 05-01 (GitHub provider) in worktree mode: replaced the throwing Phase-3 stub at `src/providers/github.ts` with a real 8-step HMAC-SHA256 validator (D-13 step order, D-07 Buffer-direct HMAC with no UTF-8 round-trip, D-10 three-way auth-header split fixing Phase 4 WR-03 from day one, D-03 `sha256=` prefix + hex-tail parse, D-04 `Buffer.from(headerHex, 'hex')` ↔ `timingSafeCompare` byte compare, D-08/D-09 `X-GitHub-Delivery` array-collapse with missing→`''`, D-11 `eventId === deliveryId`, D-12 `Math.floor(Date.now()/1000)` receipt timestamp); changed `const githubProvider` → `export const githubProvider` for the test import; created `src/providers/github.test.ts` with 13 `it(...)` blocks pinning SC1/SC2/SC5 + D-14 outer-`toThrow` guards on every error case (WR-02 fix from day one) using `node:crypto.createHmac` directly to keep tests independent of the library `computeHmac`. `src/errors.ts` byte-for-byte unchanged; `grep -cE "x-hub-signature[^-]" src/providers/github.ts` returns 0 (D-02 deprecated SHA-1 header invisible). 99 tests green across 12 files; tsc strict-mode clean. Task: `05-01`. Commit: `a96ae16`.
- **2026-05-08** — Executed Plan 05-02 (Shopify Provider) in worktree: replaced the Phase 3 throwing stub at `src/providers/shopify.ts` with the real 8-step HMAC-SHA256 validator (D-13 order: rawBody guard → D-10 three-way auth-header split → D-07 Buffer-direct HMAC over `req.rawBody` → D-06 loose `Buffer.from(header,'base64')` + `timingSafeCompare` whose length-mismatch path naturally rejects D-05 hex-as-base64 as `'signature_mismatch'` — NO union widening — → D-08/D-09 metadata array-collapse for X-Shopify-Topic + X-Shopify-Webhook-Id → D-13 step 7 JSON.parse-after-success → D-11/D-12 ShopifyWebhook build with `eventId === webhookId` and receipt timestamp); changed `const shopifyProvider` → `export const shopifyProvider` (only API-surface delta); preserved `registerProvider('shopify', shopifyProvider)` self-registration. Created `src/providers/shopify.test.ts` (260 lines, 11 `it` blocks) with mandatory D-14 outer `expect(() => ...).toThrow(WebhookValidationError)` guard on all 7 error-case tests (Phase 4 WR-02 day-one fix); the hex-as-base64 test asserts `.reason !== 'invalid_encoding'` and the array-header test asserts `.reason !== 'missing_header'` (Phase 4 WR-03 day-one fix). `src/errors.ts` byte-for-byte unchanged (union still 6 members). Full suite 86 → 97 tests green across 12 files, `tsc --noEmit` exit 0. SHOP-01 and SHOP-02 satisfied. Task: `05-02`. Commit: `7fb70b4`.
- **2026-05-08** — Executed Phase 5 end-to-end via `/gsd-execute-phase 5`: 1 wave of worktree-isolated executor agents (2 plans, fully parallel — 05-01 GitHub + 05-02 Shopify), per-wave merge with manual log.md union resolution across both worktrees, post-merge build/typecheck/test gates green (110 tests across 13 files; 86 → 110 = +24 from Phase 5: 13 GitHub + 11 Shopify); gsd-code-review surfaced 3 advisory warnings (WR-01 empty-string asymmetry untested; WR-02 leakage assertion shape; WR-03 leakage assertion not on tampered path) — 0 blockers, all advisory; gsd-verifier confirmed 5/5 must-haves verified, status=passed; flipped ROADMAP top-level Phase 5 [ ] → [x] and Progress table 0/2 → 2/2 Complete; flipped REQUIREMENTS.md GHUB-01..03 + SHOP-01..02 to [x] Complete; STATE.md → phase_complete (5/7 phases, 71%, current focus → Phase 06); evolved PROJECT.md (GitHub + Shopify rows promoted Active → Validated, Last-updated footer rewritten). Final state: 110 tests green across 13 files, 0 critical findings, 3 advisory warnings tracked in 05-REVIEW.md for Phase 6 audit. Task: `phase-5-execute`. Commit: `5977253`.
- **2026-05-08** — Extended `.gitignore` to cover 20 untracked entries (`.claude/.progress-state`, `.claude/agents/`, `.claude/commands/`, `.claude/get-shit-done/`, `.claude/gsd-file-manifest.json`, `.claude/hooks/gsd-*`, `.claude/hooks/show-progress.sh`, `.claude/package.json`, `run-claude-dangerously.sh`) that have been carried untracked across multiple sessions — per-user GSD machinery and a dangerous-mode run script, not project artifacts. Tracked exceptions preserved: `.claude/settings.json` (project-shared) and `.claude/hooks/check-daily-plan.py` (daily-plan SessionStart hook). `git status` now reports a clean working tree at end-of-day after Phase 5 close. Task: `gitignore-gsd-machinery`. Commit: `533ba7c`.
- **2026-05-09** — Captured Phase 6 (Integration Tests, Coverage Gate & Negative-Case Audit) context via `/gsd-discuss-phase 6` covering 4 selected gray areas. Coverage tooling: `@vitest/coverage-v8`, strict ROADMAP scope (`src/crypto/`, `src/providers/`, `src/middleware.ts`), per-file `coverage.thresholds.perFile=true` at 90, single canonical CI cell (Node 22 × Express 5.x), disallow `/* v8 ignore */`. Integration suite: 3 files at `tests/integration/{stripe,github,shopify}.test.ts` (location pre-locked by P1 D-05), per-test `makeApp` factory, BOTH body-parser modes per provider (BODY-01 + BODY-02), inline `makeSignature` helpers. Audit-fix scope: ALL 8 carry-over advisories in-scope (P4 WR-01 NaN tolerance loud-fail at factory, P4 WR-02 outer-toThrow guards on 4 vacuous tests, P4 WR-03 array-header three-way split, P4 WR-04 default-tolerance dedupe, P4 WR-05 strict-numeric `t=` parser, P5 WR-01 empty-string sig tests, P5 WR-02 vacuous body-leakage assertion fix, P5 WR-03 defense-in-depth leakage assertions). Guard-removal verification: manual 5-mutation experiment at integration tier, sequential edit→test→revert→next discipline (no commits), evidence in 06-VERIFICATION.md "Guard Removal Demonstrations" section. 19 implementation decisions (D-01..D-19) + Claude's Discretion + 17 deferred ideas captured in 06-CONTEXT.md; 06-DISCUSSION-LOG.md preserves the alternatives. STATE.md updated to record session. Task: `phase-6-discuss`. Commit: `bb1667d`.
- **2026-05-29** — Captured a Phase 7 security-notes seed (`07-SECURITY-NOTES-SEED.md`) documenting the test-oracle limitation: the Phase 6 hand-rolled `makeSignature` suite (06 D-11) shares an author/spec-reading with the validator, so it proves Express wiring + regression safety but NOT spec conformance against real providers (shared-oracle / common-mode failure). Seed prescribes README guidance to validate against a real source once (Stripe CLI / `generateTestHeaderString`, GitHub redeliver, Shopify sandbox; captured fixtures = ground truth). Task: `phase-7-security-notes-seed`. Commit: `1dfff46`.
- **2026-05-29** — Created Phase 6 plan set (4 PLAN.md files in 2 waves): Plan 01 coverage tooling (`@vitest/coverage-v8`, vitest.config.ts per-file gate, CI step), Plan 02 Supertest integration suite (3 files × 2 body-parser modes per provider), Plan 03 audit-fix bundle (all 8 carry-over advisories D-12..D-16 across 6 files), Plan 04 mutation experiment + 06-VERIFICATION.md evidence (D-17..D-19); ROADMAP.md Phase 6 plan list updated. Task: `phase-6-plan`. Commit: `741468f`.
- **2026-05-29** — Captured CI-ENFORCE deferred item in STATE.md Pending Todos: CI runs on push/PR so it detects failures only after they land on `main` (no branch protection, no required checks, no pre-push hook; the Phase 6 coverage gate inherits this — red signal, blocks nothing). Recorded as a deliberate solo-portfolio scope call (fix-forward) deferred to v2, with the closing options (branch protection + required check + PR flow, or pre-push hook). Surfaced while explaining the coverage-gate timing. Task: `capture-ci-enforce-deferral`. Commit: `8e5a297`.
- **2026-05-29** — Installed `@vitest/coverage-v8`, added `test:coverage` npm script, configured per-file >90% V8 coverage gate in `vitest.config.ts` (D-01..D-06: exact three-path include, perFile:true, all four metrics at 90), and added a coverage CI step gated to Node 22 x Express 5.x only via `if:` conditional in `.github/workflows/test.yml`. Satisfies QUAL-03. `npm run test:coverage` exits 0 with 110 tests passing. Task: `06-01`. Commit: `a82673a`.
- **2026-05-29** — Created three Supertest integration test files (`tests/integration/{stripe,github,shopify}.test.ts`) each with two `describe()` groups (rawBodyCapture mode BODY-01 and verifyCallback mode BODY-02); 16 integration tests total covering valid sig, tampered body, missing header, Stripe replay, and Shopify hex-encoding rejection; auto-fixed invalid JSON in Shopify tampered payload (`0000` → `8888`). Full suite 126 tests green across 16 files. Satisfies QUAL-02, BODY-01, BODY-02 at integration tier. Task: `06-02`. Commit: `29f2823`.
- **2026-05-29** — Closed all 8 advisory carry-overs (P4 WR-01..WR-05 + P5 WR-01..WR-03) across 6 files: D-12 three-way Stripe header split (array→invalid_signature_format), D-13 tolerance NaN/non-finite/negative loud-fail at factory call time (+5 middleware tests), D-14 strict-numeric t= regex gate, D-15 WR-04 toleranceSeconds default removed from stripe.ts, D-15 P4 WR-02 outer toThrow guards on 4 vacuous tests, D-15 P5 WR-01 empty-string sig asymmetry tests (GitHub→invalid_signature_format, Shopify→signature_mismatch), D-15 P5 WR-02 non-vacuous body-leakage assertions, D-16 defense-in-depth str leakage assertions. All 135 tests green. Satisfies QUAL-01, QUAL-04. Task: `06-03`. Commit: `a94558d`.
- **2026-05-29** — Wave 1 post-merge gate caught two cross-plan regressions that `npm test` (vitest, no type-check) had masked. (1) Type-soundness: 06-03's WR-04 removal of the `toleranceSeconds` default made stripe `validate` a required-3-arg method, no longer assignable to the deliberately-2-arg `Provider` contract (D-16) — `tsc`/DTS build failed (TS2322 + 7× TS2554 at test call-sites). Fixed by making `toleranceSeconds?: number` optional (restores `Provider` assignability) plus a fail-loud guard before the timestamp check (honors WR-04 "no default in stripe.ts" without silently disabling the replay window), and routing the tests' tolerance calls through a typed `validateWithTolerance` cast view — the same indirection the middleware uses. (2) CI lint: `biome check` failed (15 errors: `organizeImports` on pre-existing source files + new 06-02 integration files, `useTemplate`, `noNonNullAssertion`); auto-fixed with `biome check --write` (90 `noExplicitAny` warnings remain, non-blocking). All five CI gates now green: lint, typecheck, build, test (135/135), coverage (per-file >90%). No pre-commit hook exists, so these slipped past the executors. Task: `phase-6-execute`. Subtask: `wave-1-post-merge-fix`. Commit: `41c8fa8`.
- **2026-05-29** — Executed Plan 06-04 mutation experiment (D-17..D-19): 5 sequential guard mutations with no commits, each reverting via `git checkout -- <file>` and confirming `git status` clean before the next. Mutations 1, 2, 4, 5 each failed integration tests (BODY-01/BODY-02 tiers); Mutation 3 (missing-header guard) revealed a gap — the integration test only checks HTTP 401 status, and the fallthrough guard also returns 401, so only the unit test caught the reason change. All source files restored byte-for-byte; final `git status` clean; `npm test` green at 135/135. Created `06-VERIFICATION.md` with SC1–SC4 evidence + Guard Removal Demonstrations 5-row table. Satisfies QUAL-04 / ROADMAP SC3. Task: `06-04`. Commit: `_pending_`.
